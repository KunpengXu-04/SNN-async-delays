# Claims ledger

Status labels: **supported narrowly**, **exploratory**, **contradicted**,
**invalidated by protocol/provenance**, and **not tested**.

| Claim | Status | Current evidence / reason | Required next evidence |
|---|---|---|---|
| The production sigmoid delay parameter is numerically incapable of long-range movement | contradicted by Levels 0A-0D | Level 0A direct recovery passes 15/15; Level 0C soft centroid passes 30/30; the selected Level 0D hard-output bridge passes 15/15 | This remains a one-synapse result; Level 1A must test task-level credit |
| The current `.001/200-step` delay recipe can move a low-initialized scalar delay to a long interior target | contradicted | Level 0A ends `3.8611` steps from target and the Level-0B analogue fails all four arms. Level 0C needs `.05`; `.01` recovers only 6/15 per selected path | Do not silently raise task LR. `.05` is authorized only for the Level-0D mechanism bridge, not XOR |
| The production circular delay buffer blocks functional timing gradients | contradicted narrowly | Buffer-current centroid recovers 15/15 in Levels 0B/0C, and the selected Level 0D hard-output/current bridge is 15/15 with 13/13 correct directions | Buffer and one-event hard-output success still do not validate task BCE or multi-synapse credit |
| The current causal filtered-trace objective provides reliable bidirectional long-range delay credit | path-dependent; contradicted for impulse/hard-spike observation | It is 5/15 on buffer current and 10/15 on hard-spike paths in Levels 0B/0D, but 15/15 on the Level-0C passive membrane trace; Level 0D finds five wrong hard-only directions | Retain a separately declared continuous timing auxiliary in Level 1A |
| A normalized centroid of one hard LIF spike is a generally usable timing loss | contradicted | Hard-LIF centroid recovers only 3/15 and has zero initial raw-delay gradient in 8/13 misaligned pairs because hard spike time is piecewise constant. Soft centroid succeeds 30/30 in Level 0C | Keep hard spikes as the evaluated interface and use a prespecified continuous-state auxiliary in Level 0D |
| A global soft centroid can provide bidirectional scalar-delay credit through the production buffer and passive LIF membrane | supported narrowly by Levels 0C-0D | Level 0C recovers all 30 subthreshold cells; Level 0D current centroid also supports a 15/15 hard-spike endpoint, while pre-reset centroid fails | Test task-loss compatibility in Level 1A; one-event centroid recovery is not task learning |
| The causal filtered-trace direction failure is independent of observed state | contradicted | It remains 5/15 with 5/13 wrong directions on buffer impulses, but reaches 15/15 with 13/13 correct directions after subthreshold LIF membrane smoothing | Treat auxiliary source and loss jointly; do not transfer either path result to hard-reset dynamics without Level 0D |
| A global symmetric kernel similarity necessarily aligns the full membrane trace | contradicted | It recovers 15/15 current cells but 0/15 membrane cells under both parameterizations, usually shifting the learned delay about one step late; one pair has a wrong initial direction | Keep an objective-independent W1/hard-arrival endpoint; similarity loss alone cannot certify timing |
| Level 0C soft-trace recovery establishes XOR or learned routing | contradicted by scope | The experiment has one fixed-weight synapse, one event, no labels, no hard spikes and no trainable weights | Pass Level 0D hard-output/soft-credit bridge, then preregister a separate K=1 XOR protocol |
| A current-centroid auxiliary can train a production delay while final evaluation uses a hard LIF spike | supported narrowly by Level 0D | Current-only and hard-plus-current `.1/1` each recover 15/15; the selected `.1` combined arm has 13/13 correct nonzero initial total-gradient directions | Preregister Level 1A K=1 XOR; lock task/auxiliary scaling and retain hard-output evaluation |
| Production pre-reset voltage is a smoother, preferable delay-credit source than synaptic current | contradicted | Pre-reset-only recovers 3/15 and is flat in 8/13 misaligned pairs; no combined pre-reset weight exceeds the hard-only 10/15 | Do not use pre-reset centroid as the default auxiliary; a materially different state estimator needs a new diagnostic |
| Any small current timing auxiliary repairs hard filtered-spike credit | contradicted | `lambda=.01` remains 10/15 with four wrong directions, whereas `.1` and `1` recover 15/15 | Treat relative gradient scale as a protocol factor; do not transfer `.1` to XOR without calibration |
| Level 0D establishes XOR, WAD or temporal resource compression | contradicted by scope | It has one fixed-weight synapse, one event, no labels, no competing inputs and no generalization split | Pass a K=1 task-level bridge before increasing K or opening a Pareto surface |
| A fixed temporal schedule improves the MLP-decoded spatial-versus-temporal hidden-area/update frontier | supported narrowly; exploratory | All 160 cells completed. At the robust `.90` rule, spatial d0 needs 48 total hidden neurons while the fixed oracle needs 24 at the same `T=18`; both seeds have worst-balanced/exact 1.0. Dense MACs, measured events, delay-value storage and decoder MACs are unchanged, while oracle delay-buffer memory is 5x | Replicate with at least five new seeds and timing perturbations; this remains a handcrafted-schedule MLP result, not a learned-delay or spiking-output claim |
| The frozen unconstrained WAD learns the two-window routing schedule in the MLP scaffold | contradicted | WAD worst-balanced is exactly `.5` in all 32 cells; query-1 BAcc is always `.5`, no selected or final WAD checkpoint has query-1 delays reaching window 1, and window-1 hidden activity is zero throughout | A new preregistered oracle-to-WAD bridge must create delayed coverage before any reliability or Pareto claim |
| The shared opponent temporal interface has delayed event support and trainable gradients under WAD | supported narrowly | Held-out preflight v2 passes every current/arrival/activity/gradient gate separately in seeds 1 and 42; minimum third-window arrival mass is .051/.084 and gradients remain nonzero | Preserve these diagnostics in the counterbalanced performance protocol |
| WAD currently performs reliable three-window temporal routing | contradicted | In the completed 45-cell counterbalanced matrix, WAD's primary minimum is `.5` in every new seed and no condition passes the routing gate | Do not open test or scale K/N/T; a materially new coverage-aware method requires a new preregistration |
| Window/class-balanced BCE is sufficient to solve the mixed temporal task | contradicted | It aligns the objective with balanced evaluation but WAD and scaffold still have worst-window balanced .5 in both held-out seeds | Counterbalanced order and matched performance study; do not treat loss correction as a method result |
| The frozen WAD recipe is viable for three-window shared temporal routing | not established; v1 gate invalidated as proxy | V1 formally failed hidden emission `[1,.606,0]`, but the checkpoint audit finds third-window output-current support `.703`, output activity `.344`, realized arrival mass `.117`, and nonzero window-specific gradients; balanced accuracy remains .5 | Held-out preflight v2 with output-arrival/current gates and balanced objective before any performance matrix |
| WAD has no delayed events reaching temporal window 3 | contradicted by checkpoint audit | Despite zero same-window hidden emission, 70.3% of exhaustive trials have third-window output current and 11.7% of realized arrival mass lands there | Replicate the mechanism-valid support measures on held-out seeds |
| The temporal preflight failure is a global simulator/output implementation bug | contradicted | Scaffold and full-support fixed delay cover all three windows with nonzero output gradients; only WAD's late support gate fails | Audit window-specific path arrivals and gradients to localize the WAD optimization geometry |
| WAD improves simultaneous spatial mixed-operation reliability | contradicted on the primary endpoint | In the completed 36-cell spatial pilot, primary linear WAD worst-balanced is .956 versus d0 .961 and fixed matched .960; paired WAD-minus-d0 mean is -.0043 and only 1/3 seeds is positive | Do not claim superiority; an equivalence claim would require a preregistered margin and substantially more seeds |
| WAD improves joint decisions with a direct spiking opponent output | exploratory | WAD-minus-scalar exact-trial is positive in 3/3 seeds (mean +.110) and output failures fall, but worst-balanced gain is only +.0014 with large variance and WAD uses more events | Replicate only if temporal preflight passes; preregister exact-trial and resource-matched controls |
| The simultaneous spatial control establishes temporal routing | contradicted by protocol | Dedicated input identities and dedicated output heads/pairs solve a spatial parallel problem and contain no shared temporal output route | Only a viable shared-output, window-specific temporal protocol can test this claim |
| The negative XOR WAD result is explained by an obvious optimizer/threshold failure | contradicted within optimization audit | Threshold .3 has viable activity, nonzero gradients, material delay motion and zero saturation; all six alternative Stage-B interventions lower mean WAD worst-query relative to baseline | A materially different method could be new work, but the preregistered rescue programme failed |
| WAD outperforms tested controls on average in counterbalanced shared temporal routing | exploratory | WAD mean operation-position balanced accuracy is `.734` versus `.656` for fixed-full; exact-trial is `.411` versus `.326`, positive in all three paired seeds. WAD nevertheless has chance-level late cells and higher measured event count | Replicate only after a coverage-aware method decision; average performance cannot replace the locked worst-cell endpoint |
| The old NOR/window-2 failure is caused only by NOR being intrinsically difficult | contradicted | Counterbalancing shows WAD learns NOR in positions 0/1 but leaves both NOR and XOR at chance in position 2; NAND is partly learnable late | Treat temporal position as the primary bottleneck and model operation-by-position interaction explicitly |
| Full delay support is sufficient for reliable counterbalanced routing | contradicted | Fixed-full has strong late-position mean `.783` but weak early mean `.519` and primary minima `.500/.486/.482` | Test timing-support allocation, not support range alone |
| A shared delay-enabled hidden layer improves the spatial-versus-temporal resource-reliability Pareto frontier | not tested; blocked at baseline gate | Phase-0 Stage A completed but no single-query hidden width passed every output-interface gate in all three seeds, so the K=2 comparison was correctly not launched | Establish a seed-robust opponent-spike baseline in a new versioned calibration before materializing the locked K=2 matrix |
| The current equal-event one-hot plus opponent target-spike interface is a seed-robust XOR baseline | contradicted operationally | No width among 4/8/12/16/24 passes every locked gate. Audit finds 7 silent cells, 2 collision cells, and only 6 successful final cells; 7/10 silent patterns have no hidden spikes, while all collision errors are early wrong spikes at t=10 before the correct t=11 spike | Preregister a new calibration separating exact truth-table batching, global one-target/no-early-spike supervision, and micro-burst structure with held-out seeds |
| A different fixed output threshold alone rescues the Stage-A interface | contradicted in exploratory checkpoint probe | Replaying final checkpoints at thresholds .015-.06 yields no width that succeeds in all three seeds; lowering helps one cell without removing collisions, while raising sharply increases silence | Treat threshold as a robustness factor inside a new training protocol, not as a post-hoc repair |
| Hidden-neuron compression implies compute compression | contradicted analytically | `h'/(K h)` measures area only; latency yields `h'T_B/(K hT_A)`, while dense shared connectivity yields synaptic-compute ratio `h'T_B/(hT_A)` | Report all ratios plus measured ledger; never use area compression as total cost |
| Output-interface calibration v2 establishes temporal routing | contradicted | It calibrates excitability only: temporal worst-balanced is .5 for every seed/threshold; at selected .2 XOR and NOR are .5 in all seeds | Only the locked paired temporal pilot can test routing; the scaffold cannot support the claim |
| WAD outperforms the strongest non-learned delay control on burst XOR | contradicted within control matrix | Primary linear scalar=.699, WAD=.678; paired WAD−scalar mean −.0208 and only 1/5 positive. Stress scalar=.652, WAD=.644 | Do not revive without a new task/protocol and matched causal controls |
| Learned WAD placement is functionally used by its trained network | supported narrowly | Delay shuffle preserves weights/decoder/multiset but lowers worst-query in 15/15 checkpoints; primary-linear mean drop .116 | Retrain weights after shuffle and use cross-fitted banks to separate placement from co-adaptation |
| Fixed heterogeneous delay is fairly ruled out | not tested | Current uniform 0–dmax bank has mean ~15 and SD ~8, unlike WAD mean ~3.5 and range ~1–9 | Cross-fitted WAD-multiset or preregistered narrow matched bank |
| WAD provides a resource-frontier advantage over scalar | contradicted within control matrix | Scalar has higher primary worst-query with 1 vs 70 delay values and 179 vs 248 scalar storage; WAD only has slightly fewer spikes and tiny exact-trial difference | Hardware-calibrated cost plus a reliability advantage would be required |
| All-time-trained models depend on early activity | supported narrowly | Late-window probe lowers worst-query in every one of 60 checkpoints; all conditions collapse near chance | Separately trained late-window models are needed for achievable-interface comparison |
| WAD has a stable scaling advantage across K/N under all-time linear burst XOR | contradicted within calibration | Validation-only 54-cell calibration: WAD−d0 worst-query shrinks from about +.078 at K=2,N=20/35 to +.013/−.031 at K=3,N=35/50 and is near zero at K=4 | Full delay-control matrix at locked non-saturated points; no test claim yet |
| K=3,N=35 is a usable non-saturated primary setting | supported narrowly | Both WAD and d0 meet preregistered validation target/variance rule; exact-trial remains well below ceiling | Freeze full controls and confirm without reusing validation for inference |
| K=4,N=50 is a usable stress setting | supported narrowly | Both conditions meet worst-query rule at the lower boundary; exact-trial is only about .23 | Treat as stress test, not sole primary endpoint |
| Learned delay improves pooled accuracy in the current late-readout simulator | supported narrowly | Existing d0 and learned-delay comparisons, but interface is asymmetric | Re-evaluate under matched readout interfaces and controls |
| WAD has a general performance advantage over d0 under uncensored observation | contradicted within XOR pilot | At K=2/h=50 burst XOR, all-time MLP gives 100% worst-query/exact-trial for both; all-time linear differs only slightly | Test fair controls at additional K/tasks with ≥5 seeds |
| WAD's late-window advantage reflects alignment | supported narrowly | XOR pilot: WAD exceeds d0 only when final-window censoring is imposed; diagnostics show learned late arrivals | Delay shuffle/fixed-bank/window-shift causal controls |
| Delay yields fixed-resource capacity gain | invalidated by protocol | `T`, output width, MLP size, and delay range grow with `K` | Matched resource ledger and Pareto analysis |
| The model temporally multiplexes rather than aligns events | not tested | Late-window design is compatible with alignment alone | Window shift, delay shuffle, query-local/all-window controls |
| WAD saves 19% spikes / 73% energy | invalidated by provenance and metric | Historical Step 1 numbers disagree with canonical d0 results; spike proxy is incomplete | Canonical recomputation with resource accounting |
| Max-K is 3/4/5 at 90% | invalidated by fresh re-evaluation | Full NAND burst Plan-D re-evaluation finds WAD only through K=2 when every available seed must clear worst-query 90%; K>=3 fails through tested h=150 | Revisit only under a new, predeclared fair-interface protocol |
| Burst encoding reveals a timing mechanism | exploratory | It is a controlled timing probe, but differs materially from rate coding | Fixed-event burst protocol and cross-encoding robustness |
| MLP exposes nonlinear hidden representation | not tested | Encoders were trained separately | Frozen representation probes |
| Homeostasis breaks sparsity--accuracy tradeoff | exploratory | Selected, low-seed observations | Predeclared multi-seed study and useful-window activity analysis |
| A global shared delay can be learned jointly with K=1 XOR when supplied an explicit timing scaffold | supported narrowly by Level 1A | The selected `lambda=.01,lr_d=.01` arm passes 10/10 across both initial directions and five seeds; max final error is `.002628` step and every hard-spike truth table is exact | Level 1B must test granularity and burst robustness; this is not task-derived routing |
| The revised K=1 XOR hard-spike interface is seed-robust under fixed d0 and fixed delay 4 | supported narrowly by Level-1A Stage I | The unique selected candidate `eta=0,lr_w=.01` passes all 10 schedule/seed cells with exact target spike trains, zero silence/collision and nonzero hidden activity | This is exhaustive training-domain optimization; delay learning and held-out generalization remain separate questions |
| A one-event arrival-centroid auxiliary is compatible with a multi-pattern task loss and trainable weights | supported narrowly by Level 1A | Every positive-lambda candidate passes 10/10; the smallest selected `.01` arm has correct total initial direction in 10/10 despite task/auxiliary conflict in 5/10 | Test whether this survives delay granularity and consecutive micro-bursts at K=1 |
| Hard-spike XOR task loss alone reliably learns the declared delay-4 schedule | contradicted by Level 1A | Task-only passes 0/10 at `lr_d=.01` and 1/10 at `.05`; only 5/10 initial task-gradient directions are correct at either LR | A materially different task-derived credit method requires a new preregistration; do not describe the timing scaffold as autonomous routing |
| Fixed d0 weights can trivially imitate the delayed target-at-15 endpoint | contradicted within Level 1A | The fixed-d0/wrong-target control passes 0/5 and has zero correct-target-time rate in every seed | Retain this timing-specificity control in Level 1B |
| The Level-1A encoding validates robustness to the earlier two-event micro-burst | not tested by design | Each selected one-hot channel emits one event at `t=9`, giving exactly two simultaneous input events per XOR pattern; consecutive within-channel bursts are excluded | If Level 1A passes, preregister Level 1B burst-structure robustness without changing the Level-1A result |
| The Level-1A scaffold remains seed-robust when delay dimensionality increases from 1 to 16 or 64 independent coordinates under the frozen optimization recipe | contradicted by Level 1B Stage A | Global passes 10/10, but per-hidden and per-synapse pass only 2/10 and 0/10; per-hidden preserves the exact interface in 10/10 but fails coordinate recovery | A dimension-scaled teacher would be a new protocol; do not reinterpret the failed registered candidates |
| The Level-1A global scaffold replicates on new initialization seeds | supported narrowly by Level 1B Stage A | Global `lambda=.01` passes 10/10 across both initial directions and five new seeds; maximum final delay error is `.001864` step | Proceed to fixed micro-burst controls only; this remains explicit oracle supervision on the exhaustive training domain |
| A fixed delay-4 K=1 XOR interface survives two consecutive input events per selected channel without early or duplicate outputs | supported narrowly by Level 1B controls | Fixed d4 passes 5/5: all 20 patterns have one exact output at `t=15`, zero silence/collision and balanced accuracy one | This is an experimenter-supplied schedule, not learned routing |
| Fixed d0 can imitate the delayed micro-burst target through weight training alone | contradicted by Level 1B controls | d0 passes 0/5; all 27 emitted spikes occur at `t=11`, none at target `t=15`, although every pattern has hidden activity | Retain as timing-specificity negative; do not reduce the gate to classification accuracy |
| A learned delay-4 K=1 XOR interface survives the consecutive micro-burst | not tested; mechanically authorized | Fixed d4 passed the 5/5 feasibility gate, but the frozen 60 learned cells have not run; Stage A already found high-dimensional optimization failures | Either execute the unchanged registered matrix or preregister a separate dimension-aware rescue; do not modify the matrix post hoc |
| The Level-1B per-hidden/per-synapse failure is explained by the mean scaffold's `1/P` per-coordinate gradient scaling | supported narrowly by R1 and sealed R3 | New-seed unscaled baselines pass 1/10 and 0/10, while analytic `lambda_P=.01P` conditions pass 10/10 in R1 and again 10/10 in sealed R3; weighted arrival gradients match the global per-coordinate magnitude and no clipping occurs | Restrict the claim to homogeneous explicit-oracle delay recovery; do not extrapolate to task-derived routing |
| Dimension-matched oracle supervision makes higher-dimensional delays seed-robust | supported narrowly | Per-hidden and per-synapse both pass all ten sealed R3 cells across both initialization sides, with exact interface, correct nonzero coordinate directions and full `.1`-step coverage | Disclose that total oracle strength grows with P; new protocols are required for micro-burst, heterogeneous routing and K>1 |
| Mean learned delay is sufficient evidence that a high-dimensional schedule was recovered | rejected as an admissible Level-1B criterion | A mean of four can hide oppositely misaligned coordinates; Level 1B therefore requires maximum coordinate error at most `.1` and coverage fraction one | Always report the full coordinate distribution, maximum error and coverage; do not substitute a mean-only claim |
| A Level-1A pass would establish temporal multiplexing or a resource Pareto advantage | contradicted by protocol scope | K=1 has no multi-query sharing, the timing target is fixed, and resource outputs remain a vector rather than a scalar energy model | K>1 needs a separate causal routing protocol after the task-level and delay-granularity gates |
